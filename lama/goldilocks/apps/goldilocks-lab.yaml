apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: goldilocks
    namespace: argocd
spec:
    destination:
        namespace: lama
        server: https://kubernetes.default.svc
    project: default
    source:
        path: lama/goldilocks
        targetRevision: bootstrap
        helm:
            valueFiles:
                - values.yaml
                - values-lab.yaml
        repoURL: https://github.com/chris-sanders/argocd.git
    syncPolicy:
        automated:
            prune: true
        syncOptions:
          - CreateNamespace=true
