istioTemplates:
  jwtIssuer: "https://keycloak.in.lab/auth/realms/basic"
  # http service uses 443 so traefik uses TLS on the backend
  jwksUri: "http://keycloak.in.lab/auth/realms/basic/protocol/openid-connect/certs"
  credentialName: istio-default-cert # must be the same as secret
  apps:
  - bitwarden-lab
  #- gollum-lab
  #- dex-lab
  - oauth-lab
  - keycloak-lab
  strictNamespaces:
  - oauth2-proxy
  #- dex
  - gollum
  - keycloak
